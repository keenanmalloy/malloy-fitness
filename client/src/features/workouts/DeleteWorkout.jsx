import React from "react";

/**
 * This component should make a DELETE request to remove the workout.
 * DELETE /workouts/:workoutId
 *
 * Component behaviour:
 * 1. User clicks button
 * 2. Button opens modal with details about deleting the workout and a confirmation button to send the request
 * 3. Clicking the confirmation button deletes the workout.
 *
 * We already have this logic elsewhere, but we should centralize it in this component.
 */
export const DeleteWorkout = ({ workoutId }) => {
  return <div>DeleteWorkout</div>;
};
